<h1>Link Your Actor To The Correct Event</h1>

<%= form_for @location do |f| %>
<%= f.collection_select :actor_id, Actor.all, :id, :name %>
<%= f.collection_select :event_id, Event.all, :id, :name %>
<%= f.submit "Connect Actor And Event" %>
<% end %>

